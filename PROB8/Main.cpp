#include "KruskalAlgo.h"
#include "app.h"
using namespace std;

int main(int argc, char* argv[])
{
	/*
	Graph graph;
	graph.nodes.push_back("0");
	graph.nodes.push_back("1");
	graph.nodes.push_back("2");
	graph.nodes.push_back("3");
	graph.nodes.push_back("4");
	graph.nodes.push_back("5");

	graph.edges.push_back({ 0, 1, 6 });
	graph.edges.push_back({ 0, 2, 1 });
	graph.edges.push_back({ 0, 3, 5 });
	graph.edges.push_back({ 1, 2, 5 });
	graph.edges.push_back({ 1, 4, 3 });
	graph.edges.push_back({ 2, 4, 5 });
	graph.edges.push_back({ 2, 3, 7 });
	graph.edges.push_back({ 2, 5, 4 });
	graph.edges.push_back({ 3, 5, 2 });
	graph.edges.push_back({ 4, 5, 6 });

	std::sort(graph.edges.begin(), graph.edges.end());
	
	kruskalAlgorithm(graph);
	*/
	
	Application app;
	while (app.getOperation()) {
		
	}
	return 0;
}